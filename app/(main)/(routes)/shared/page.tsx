"use client";

import EmptyPage from "@/components/empty-page";

export default function SharedFilesPage() {
    return (
        <EmptyPage
            iconName="FolderOpen"
            contentHeader="No files shared yet"
            content="Invite your team members to share files, or upload your own to get started."
        />
    )
}